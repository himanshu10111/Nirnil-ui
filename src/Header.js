// src/Header.js
import React from 'react';
import './Header.css'; // Assuming you have a separate CSS file for styling

const Header = () => (
  <header className="header">
    <h1>My Website</h1>
    {/* Navigation or any other content can be added here */}
  </header>
);

export default Header;
